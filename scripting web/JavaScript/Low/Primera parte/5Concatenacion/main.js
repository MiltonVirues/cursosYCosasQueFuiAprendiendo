/*numero1="5";
numero2=8;
frase= numero1.concat(numero2);// si a esto lo sumo seria como una operacion de suma tipica pero cuando hago lo siguiente

// frase= "" + numero1 + numero2;  esta suma se convierte en una concatenacion de strings
 //El objetivo de haber mostrado eso eso, es que vamos a utilizar una funcion para concatenar strings de manera mas dinamica con el siguiente ejemplo
//nota: para utilizar el concat, si o si necesitamos un String. porque es un "metodo de cadena"(que se va a ver mas adelante)
*/
//Concatenacion con backticks
/*Syntaxis:
"${nombre de la variable}" */
nombre = "milton virues"
frase = `soy ${nombre} y estoy caminando`
// los backticks son comillas graves " `` "
document.write(frase);
